//to see weather the number is even o odd  
/* if the value is 1 so its even and if its 0 then its 0 */ 
#include<stdio.h> 
#include<math.h> 
int main() 
{ 
int a; 
printf("enter the value"); 
scanf("%d",&a); 
printf("%d", a % 2 ==0); 
return 0; 
}
